services:
  backend:
    build: ./backend
    container_name: car_backend
    ports:
      - "8001:8001"

  frontend:
    build: ./frontend
    container_name: car_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
